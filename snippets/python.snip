snippet     pretty-printer
alias       pprinter ppter
options     head
    ###### pretty printer
    import pprint
    pp = pprint.PrettyPrinter(indent=4)
    pprint = pp.pprint
    def debug(log):
        print('---------------------------------------------')
        pprint(log)
        print('---------------------------------------------')${0}
    ###### pretty printer
